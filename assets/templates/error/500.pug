extends ../base.pug

block body
  | {% import "bootstrap/wtf.html" as wtf %}
  .content-section
    .outer
      .middle
        .inner
          div(style="text-align: center;")
            h1 500 Error
            h3 The server encountered an internal error. That's all we know.
            hr.intro-divider
            a(href="{{ url_for('home.homepage') }}", class="btn btn-default btn-lg") <i class="fa fa-home"></i> Home
